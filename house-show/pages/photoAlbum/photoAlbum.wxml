<!--pages/photoAlbum/photoAlbum.wxml-->
<view >
<button size='mini' bindtap="click1" class="btn">卧室</button>
<button size='mini' bindtap="click2" class="btn">卫生间</button>
<button size='mini' bindtap="click3" class="btn">客厅</button>
</view>

<view hidden="{{bedroomhidden}}" class="swiper" wx:key="id"  wx:for="{{bedroomphotos}}">
<image src="{{item.imageUrl}}" mode="scaleToFill"/>
</view>

<view hidden="{{toilethidden}}" class="swiper" wx:key="id"  wx:for="{{toiletphotos}}">
<image src="{{item.imageUrl}}" mode="scaleToFill"/>
</view>

<view hidden="{{saloonhidden}}" class="swiper" wx:key="id"  wx:for="{{saloonphotos}}">
<image src="{{item.imageUrl}}" mode="scaleToFill"/>
</view>

